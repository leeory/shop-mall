spring:
  application:
    name: service-user
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yaml
        refresh-enabled: true
        shared-configs:
          - all-service.yaml
  profiles:
    active: dev #激活开发环境的配置